import { CommonResponse } from '../../../bloben-interface/interface';
import { createCommonResponse } from '../../../utils/common';

export const logoutAdmin = async (): Promise<CommonResponse> => {
  return createCommonResponse();
};
